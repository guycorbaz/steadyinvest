//! View-model helpers: presentation-only transforms between core/contract data and the UI.
//! Nothing in here calculates — the Cardinal Rule keeps all arithmetic in `core`.

pub mod chart;
pub mod engine;
pub mod entry;
pub mod form;
pub mod format;
pub mod refresh;
pub mod studies;
pub mod verify;
