# generated: 2026-06-09
# last_updated: 2026-06-14
# project: steadyinvest
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
#
# REGENERATED 2026-06-09 from the post-restart epics.md (8 epics, 62 stories). Replaces the legacy
# web/Loco sprint status. PHASE NOTE: Epics 1-5 = P1 (MVP), detailed to story level; Epics 6 [P2] /
# 7 [P3] / 8 [V] are deliberate story outlines (refine with create-epics-and-stories per phase).

generated: 2026-06-09
last_updated: 2026-06-14
# 2026-06-14: story 2-7 created (low-confidence & plausibility surfacing) → ready-for-dev. app-only SURFACING
#             story (no detection — engine already computes both): (a) carries an explicit "Historique insuffisant
#             — confiance réduite" label onto the verdict badge + sticky bar from verdict.low_confidence() (<5
#             usable years, FR8); (b) renders each plausibility Finding/CalcFinding as a NEUTRAL inline glyph (△)
#             at the affected §2/§3 cell, distinct from coverage/stale/review markers AND from quality flags (FR10),
#             detail revealed on focus (NFR-U2). Key dev trap flagged: build_snapshot currently DROPS the input-
#             shape findings on CanonicalFinancials (consumed by StudySnapshot::new, not re-exposed) → must return
#             a StudyFrame{snapshot, plausibility} cloning canonical.findings BEFORE the move, NO second normalize
#             (single-frame coherence). Calc-time findings + low_confidence already on snapshot.outputs(). Concrete
#             context→cell map pinned (high_price→a, low_price→b, eps→c, dividend→f; sales/pretax/book §2); fiscal &
#             study-level (forecast_low, year=None) findings anchored, not dropped. NO new dependency (Cargo.lock +
#             deny.toml UNCHANGED); core/contract/persistence CONSUMED, NOT modified; colour budget honoured (glyph
#             + label neutral ink, never a hue). Scope-guarded: NO chart/drag (2.8), NO quality-flag rendering
#             (different concept), NO undo/redo (2.9), NO provider/reconcile (Epic 3). Interp → new GitHub issue.
# 2026-06-13: story 2-6 created (numeric judgment inputs, verdict & zone bar — integrity-gated) → ready-for-dev.
#             THE engine-wiring story: first time `app` calls `core`. Adds exact-VALUE numeric judgment entry
#             (sales/EPS growth %, est. high/low EPS, judged high/low P/E, 4-option low-price selector +
#             recent-severe-low, current price, dividend) persisted to contract::Judgment; on any data/judgment
#             edit maps Study→core::RawFinancials→normalize→CanonicalFinancials, Judgment→ssg::JudgmentInputs,
#             builds verdict::InputGates from usable-years' cell review×freshness (2.5) + judgment inputs, calls
#             StudySnapshot::new(...) ONCE per frame → renders §2/§3/§4/§5 result slots (formatted strings,
#             unknown→em-dash never 0), the §4 zone bar (FIRST saturated-colour spend: Buy/Neutral/Sell @ per-
#             theme alpha + edge strokes + present-price marker + price axis; muted entry→full contemplation),
#             U/D ratio + projected return, the verdict badge (Full / Provisional-hatched+temporal-provenance /
#             Withheld — engine's exact tri-partition), the sticky verdict bar, and a traceability view (inputs
#             →provenance→rule). Verdict integrity holds by construction (full colour ⟺ all load-bearing inputs
#             ✓ & not stale, FR12). NO new dependency (core already declared+used; Cargo.lock/deny.toml expected
#             UNCHANGED); core/contract/persistence CONSUMED, NOT modified (all API verified present: 1.7
#             normalize, 1.8 compute, 1.11 StudySnapshot, 2.2/#14 Judgment schema). Scope-guarded: NO §1
#             draggable chart / drag-recolour (2.8 — zone recolours on VALUE change), NO cell-level plausibility
#             warning (2.7 — but engine low_confidence DOES drive verdict→Provisional), NO undo/redo (2.9), NO
#             rationale (2.10), NO provider fetch / quarterly auto-fill (Epic 3 — QuarterlyObservations::empty,
#             current P/E honestly unknown). Recorded interp: judgment-input gate = None→Missing, Some→
#             ValidatedFresh (deliberate personal judgment is the user's own validated number).
# 2026-06-13: story 2-5 created (tri-state validation with soft-lock) → ready-for-dev. app-only review-axis
#             story: render + user-set the per-cell tri-state review tag (none/? to-review/✓ validated) on
#             the §2/§3 editable cells (cycle via free keyboard chord + marker click); soft-lock a ✓ cell
#             (edit refused until one deliberate clear-✓ → ? gesture, never silently blanked); "unlock all"
#             (study / per-year / per-metric) flips ✓→? behind a confirmation (the app's first
#             confirm-before-act). ✓ = geofenced sanctioned desaturated ink-green (≈#4A7C6F, the ONE colour-
#             budget exception, never co-present with zones) + reduced-motion-respecting ~120ms draw anim;
#             ? = hollow glyph on a 2nd non-colour channel (never the stale ◦ dot). Asymmetric attenuation:
#             only ✓ dims in contemplation; ?/stale/missing never attenuate. NO new dependency (Cargo.lock +
#             deny.toml expected UNCHANGED); contract NOT modified (Review enum + Cell::edited ✓→? rail
#             already exist, Story 1.11). Scope-guarded: NO engine/verdict/verdict-integrity gate (2.6), NO
#             chart (2.8), NO plausibility (2.7), NO provider/reconcile/provider-divergent auto-tag (Epic 3).
# 2026-06-13: story 2-2 dev complete → review (issue #14 closed; 4 gates green --locked).
# 2026-06-13: story 2-2 review complete → done (1 MEDIUM auto-fixed: keep input on refusal; 4 gates re-green).
# 2026-06-13: story 2-3 created (faithful collapsible SSG form §1–§5) → ready-for-dev. app-only UI-structure story;
#             scope guarded: NO engine/verdict/zone (2.6), NO chart (2.8), NO data entry (2.4), NO markers (2.5).
# 2026-06-13: story 2-3 dev complete → review. app-only; pinned surfaces + Cargo.lock untouched; 4 gates green --locked;
#             app tests 39→53; interpretations → issue #20; AC6 GUI click-through left for human/AT-SPI (sandbox).
# 2026-06-13: story 2-3 review complete → done. Adversarial review: 4 gates re-green --locked, pinned surfaces +
#             Cargo.lock re-diffed empty, 53/53 app tests. 1 LOW auto-fixed (re-selecting active regime no longer
#             clobbers manual folds); 2 LOWs recorded for 2.4/2.6. No CRITICAL/HIGH/MEDIUM.
# 2026-06-13: story 2-4 created (manual data entry with provenance & coverage) → ready-for-dev. app-only entry story:
#             editable §2/§3 grids (type + paste-a-column, locale-aware parse → contract::Money), each edit via
#             contract::Cell::edited stamped source=manual + persisted (put_study upsert); source×freshness×coverage
#             display under attention hierarchy (missing shouts, stale murmurs, source on demand), no colour spent;
#             unknown never 0. arboard dev-dep→runtime dep. Scope-guarded: NO review markers/soft-lock (2.5), NO
#             engine/verdict/zone (2.6), NO chart (2.8), NO plausibility (2.7), NO provider/reconcile (Epic 3).
#             Carries the 2.3-review LOW: fix the PE_TABLE_ROWS=5 cap (issue #20).
# 2026-06-13: story 2-4 dev started → in-progress.
# 2026-06-13: story 2-4 dev complete → review. app-only entry story; editable §2/§3 grids (type +
#             paste-a-column, locale-aware parse → contract::Money), each edit via Cell::edited stamped
#             source=manual + persisted (put_study upsert), reopen round-trip proven headlessly;
#             source×freshness×coverage under attention hierarchy, no colour spent; unknown never 0.
#             arboard+rust_decimal dev→runtime deps (Cargo.lock UNCHANGED); pinned surfaces + deny.toml
#             re-diffed empty; 4 gates green --locked; app tests 53→71. PE_TABLE_ROWS=5 cap removed
#             (issue #20); interpretations → issue #21. AC6 GUI click-through left for human/AT-SPI (sandbox).
# 2026-06-14: story 2-8 created (interactive §1 growth chart — draggable judgment line, live recolor) →
#             ready-for-dev. THE signature interaction; productionizes Spike B (1.5, GO: 40–60µs/235µs max,
#             400×–2500× under the 100ms NFR-P1 budget) and drives the §4 ZoneBar built in 2.6. app-ONLY
#             (core/contract/persistence/ingestion/report + Cargo.lock + deny.toml all pinned, re-diff empty;
#             NO new dependency). Two PHYSICALLY DISTINCT surfaces (locked): NEW §1 growth_chart.slint
#             (semi-log 1→200, solid hist/dashed proj, 5–30% guide fan, draggable trend line(s), Path+
#             TouchArea, NO zones, ink-scale ONLY — colour budget = §4 hues only) vs the EXISTING §4
#             zone_bar.slint (recolour target, do NOT rebuild). Drag → value_for_y → judgment input on a
#             NON-persisted Study clone → engine::build_frame ONCE → push zone-bar+verdict live; persist
#             (put_study) ONLY on drag-end. CENTRAL TRAP flagged: do NOT route each `moved` through
#             push_form+put_study (rebuilds every row + journals — misses 100ms); recompute is sub-ms, the
#             cost to avoid is per-event writes. Cardinal Rule: zone/forecast/verdict recompute in CORE,
#             never in a Slint callback; pixels are f32/f64 (rendering ≠ decision chain, spike-proven).
#             Task 1 extends StudyFrame with per-year canonical series CLONED before the StudySnapshot::new
#             move (2.7 plausibility pattern — NO second normalize → coherence). Gesture↔exact-value sync by
#             construction (both write same Study.judgment via set_judgment; JudgmentField re-seeds only
#             !has-focus). Verdict integrity by construction: live recolour respects the §4 sat/confidence
#             gate (full⟺all load-bearing ✓&fresh∧¬low-conf; provisional hatch; withheld empty). Spike
#             example + justfile spike-b recipe REMOVED (clippy --all-targets lints examples); spike findings
#             doc kept. Scope-guarded: NO undo/redo/scenario-compare (2.9 — incl. FR32 undo), NO rationale
#             (2.10), NO §1 zones, NO provider (Epic 3). 3 non-blocking open Qs recorded (soft-lock on
#             judgment fields; exact line→field geometry; #draggable lines in v1 — defaults chosen). DoD:
#             headless tests (coord↔value round-trip, series→Path commands, recompute coherence, persist
#             round-trip, integrity gate) + posture floors bumped; in-GUI drag click-through = documented
#             partial (human/AT-SPI sandbox), as 2.1–2.7.
# 2026-06-14: story 2-8 dev complete → review. Native-Slint §1 interactive growth chart (Path+TouchArea):
#             semi-log 1→200, Sales/EPS/Price solid historical + DASHED EPS projection (Rust-built dash —
#             Slint Path has no dash array), 5–30% guide fan, decade axis, ink-scale ONLY (no judgment hue
#             on §1). The signature gesture: drag the est-high-EPS line → value_for_y → apply to an in-mem
#             Study clone (cached at pointer-down) → engine::build_frame ONCE → push judgment+§1 chart+§4
#             zone bar+verdict LIVE; persist ONCE on pointer-up via set_judgment_field (gesture⇄exact-value
#             one source of truth, FR31). Central latency trap AVOIDED: no per-`moved` put_study/push_form.
#             Cardinal Rule kept (zone/forecast/verdict in core; only pixels are f32/f64). Verdict integrity
#             by construction: live recolour reuses §4 sat/confidence gate (full⟺all load-bearing ✓&fresh;
#             provisional hatch; withheld empty) — proven by a drag-on-withheld test. FR33: line drawn only
#             when engine has a forecast (judgment_y=-1=unset, never auto-placed). AC5: ZoneSegment animate
#             background 150ms, 0ms under reduced-motion. Series cloned off canonical BEFORE StudySnapshot::
#             new move (2.7 coherence invariant). Spike B example + justfile recipe REMOVED (findings doc
#             kept); NO dependency change (Cargo.lock/deny.toml byte-identical; core/contract/persistence/
#             ingestion/report + rust-toolchain pinned, re-diffed empty). 4 gates green --locked; app tests
#             102→110 (+8 chart: axis round-trip+clamp, path_commands, series→commands, dashed projection,
#             gesture→value→chart round-trip, live integrity gate, persist round-trip). posture floors
#             bumped (.slint 18→19, @tr 130→140). Binary launches + runs the event loop (Wayland, exit 124);
#             in-GUI pointer-drag click-through = documented partial (human/AT-SPI sandbox), as 2.1–2.7.
#             3 interpretations → GitHub issues: shared-axis series scaling (sales clamps), fixed-size chart
#             box, single draggable handle (EPS only) — all deliberate v1 scope, refinements deferred.
# 2026-06-14: story 2-8 review complete → done. Adversarial 3-layer code review (Blind/Edge/Acceptance,
#             0 failed): NO hard AC violation (Cardinal Rule, colour budget, verdict integrity, FR33,
#             gesture⇄value sync, pinned surfaces all verified clean). 0 decision · 5 patch · 7 defer · 4
#             dismiss. All 5 patches applied + 4 gates re-green --locked (app tests 110): P2 a zero-movement
#             click on the drag strip no longer rewrites the forecast (commit gated on a real `moved`); P3 a
#             refused/failed write reconciles the preview to disk (no phantom un-saved line); P4 pointer
#             `cancel` reverts instead of persisting (new judgment-cancel callback); P1 §4/§5 numbers
#             (forecast high/low, U/D, projected return, §4 warning) refresh on the live drag frame so the
#             bar + numbers stay coherent; P5 judgment-dragging defensively reset on study open + both close
#             paths (mid-drag teardown can't leave the form unscrollable). 7 deferred → deferred-work.md +
#             story Review Findings (axis 1→200 can't show sub-$1/>$200 EPS, slider a11y keyboard step,
#             grip/endpoint cosmetic offset, derived-forecast→direct conversion, single-point/all-None
#             series rendering, fixed-height coupling) — consistent with Guy's "GUI polish post-MVP". Work
#             NOT yet committed (manual dev-story this session, not story-automator). Next: commit/push 2-8,
#             then story 2.9 (undo/redo & scenario compare).
project: steadyinvest
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # ── Epic 1: Proven SSG core & data foundation (headless) [P1] ──
  epic-1: done
  1-1-workspace-scaffold-ci-gate-skeleton: done
  1-2-ssg-method-specification: done
  1-3-contract-v1-versioned-types-provenance-model: done
  1-4-spike-a-dense-editable-grid-paste-a-column: done
  1-5-spike-b-native-slint-draggable-judgment-line-recolor: done
  1-6-spike-c-exact-decimal-cagr-precision-cross-os-determinism: done
  1-7-core-normalization-layer: done
  1-8-core-ssg-calculation-engine: done
  1-9-golden-reference-studies-self-check-gate: done
  1-10-persistence-v1-hybrid-store-journal-identity-migrations: done
  1-11-verdict-integrity-coherence-invariants: done
  epic-1-retrospective: done

  # ── Epic 2: The trustworthy Stock Study (first usable product) [P1] ──
  epic-2: in-progress
  2-1-application-shell-theme-disclaimer: done
  2-2-create-save-reopen-study: done
  2-3-faithful-collapsible-ssg-form: done
  2-4-manual-data-entry-provenance-coverage: done
  2-5-tri-state-validation-soft-lock: done
  2-6-numeric-judgment-inputs-verdict-zone-bar: done
  2-7-low-confidence-plausibility-surfacing: done
  2-8-interactive-growth-chart-draggable-judgment-line: done
  2-9-undo-redo-scenario-compare: backlog
  2-10-decision-rationale-capture: backlog
  2-11-update-existing-study-extend-projection: backlog
  2-12-dashboard-search-sort-filter-archive-delete: backlog
  2-13-legend-empty-error-states-help-demo-verify-engine: backlog
  2-14-neutral-voice-banned-verb-enforcement: backlog
  epic-2-retrospective: optional

  # ── Epic 3: Provider data & reconciliation [P1] ──
  epic-3: backlog
  3-1-marketdataprovider-trait-eodhd-adapter: backlog
  3-2-provider-configuration-api-keys-keychain: backlog
  3-3-manual-refresh-recompute-freshness: backlog
  3-4-non-destructive-reconciliation: backlog
  3-5-graceful-provider-failure: backlog
  3-6-annual-update-journey: backlog
  epic-3-retrospective: optional

  # ── Epic 4: Watchlist & single-portfolio risk [P1] ──
  epic-4: backlog
  4-1-watchlist-management: backlog
  4-2-neutral-buy-zone-alerts: backlog
  4-3-single-portfolio-holdings-register: backlog
  4-4-manual-price-refresh-per-holding-zones: backlog
  4-5-trailing-stop-per-holding: backlog
  4-6-simple-capital-at-risk: backlog
  4-7-neutral-sell-stop-triggers-manual-actions: backlog
  epic-4-retrospective: optional

  # ── Epic 5: Cumulative memory & portability [P1] ──
  epic-5: backlog
  5-1-reopen-confront-past-judgment-vs-reality: backlog
  5-2-export-import-single-study: backlog
  5-3-export-import-whole-journal: backlog
  5-4-restore-from-backup: backlog
  5-5-journal-location-recent-journals-sync-safety: backlog
  5-6-pdf-export-of-study: backlog
  epic-5-retrospective: optional

  # ── Epic 6: Multi-portfolio, multi-currency & full risk overlay [P2] (story outlines) ──
  epic-6: backlog
  6-1-multiple-portfolios-one-per-bank: backlog
  6-2-multi-currency-holdings: backlog
  6-3-transaction-ledger-partial-sells-weighted-average-cost-basis: backlog
  6-4-dividends-gross-study-net-reinvestable: backlog
  6-5-fx-acquisition-consolidation: backlog
  6-6-capital-at-risk-per-currency-bank-global: backlog
  6-7-concentration-check-diversify-by-size: backlog
  6-8-replacement-candidate-surfacing-on-sell: backlog
  6-9-provider-fallback-chain-rate-limit-batching: backlog
  epic-6-retrospective: optional

  # ── Epic 7: Comparison, health review, screening & reports [P3] (story outlines) ──
  epic-7: backlog
  7-1-company-comparison: backlog
  7-2-portfolio-health-review: backlog
  7-3-discovery-screening-quick-screen: backlog
  7-4-additional-provider-adapters: backlog
  7-5-pdf-print-other-forms: backlog
  epic-7-retrospective: optional

  # ── Epic 8: Read-only AI clerk-of-memory & MCP façade [V] (story outlines) ──
  epic-8: backlog
  8-1-read-only-mcp-facade: backlog
  8-2-local-first-read-only-ai-provider-abstraction: backlog
  8-3-past-interrogation-prompts: backlog
  8-4-capability-asymmetry-tests: backlog
  epic-8-retrospective: optional
