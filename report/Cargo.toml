[package]
name = "steadyinvest-report"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
authors.workspace = true
description = "UI-independent PDF/print of the faithful, neutral, grayscale-safe SSG form."

[dependencies]
steadyinvest-core = { workspace = true }
steadyinvest-contract = { workspace = true }
rust_decimal = { workspace = true }   # exact decimals on the Study → core mapping (no f64)
# Write-only PDF backend (Story 5.6, FR52): precise coordinate control for the faithful SSG grid,
# pure-Rust, minimal deps (bitflags/itoa/memchr/ryu), no PDF parser → no untrusted-parse advisory.
pdf-writer = { workspace = true }

[dev-dependencies]
uuid = { workspace = true }   # fixture study ids in the renderer tests
